package ssGroup.mascota.service;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import ssGroup.mascota.entity.Mascota;
import ssGroup.mascota.repository.MascotaRepositorio;

//@Service
public class MascotaServicio {
    /*
    @Autowired
    private MascotaRepositorio rep;
    
    public Mascota guardarM(Mascota m){ return rep.save(m); }
    public List<Mascota> listarM(){ return rep.findAll(); }
    public Mascota obtenerM(Integer id){ return rep.findById(id); }
    */
}
